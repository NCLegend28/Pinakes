# Morgans — runtime dependencies (cleaned, deduped 2026-05-07)
# Full pinned snapshot retained in requirements.full.txt for reproducibility.

# Core data + I/O
pandas>=2.2
requests>=2.32
python-dotenv>=1.0
schedule>=1.2

# Sentiment models
vaderSentiment>=3.3
pysentiment2>=0.1.1   # Loughran-McDonald financial dictionary (SEC filings)
nltk>=3.8             # Required by pysentiment2 for tokenization/stemming

# SEC filings parsing
beautifulsoup4>=4.12

# Reddit scraping (backfill_reddit_sentiment.py)
praw>=7.8

# Charting (used in some analysis notebooks/scripts)
matplotlib>=3.8

# Market data
yfinance>=0.2
alpaca-py>=0.42

# PDF parsing (split_pdf.py)
pypdf>=4.0

# --- Optional, heavy: only needed if FINBERT_ENABLED=true in .env ---
# Uncomment to enable FinBERT scoring in sentimentBot.py.
# transformers>=4.40
# torch>=2.2
