# GPU Requirements for HealthGlimpse+
# Use this requirements file on GPU-enabled systems for optimal performance

# Core dependencies (same as base requirements)
flask==3.0.0
werkzeug==3.0.1
jinja2==3.1.2

# GPU-optimized PyTorch and ML libraries
torch==2.3.1+cu121  # CUDA 12.1 version for better GPU support
torchvision==0.18.1+cu121
torchaudio==2.3.1+cu121
--extra-index-url https://download.pytorch.org/whl/cu121

# Hugging Face libraries optimized for GPU
transformers==4.42.3
tokenizers==0.19.1
huggingface-hub==0.24.2
accelerate==0.31.0  # For device mapping and memory optimization
bitsandbytes==0.43.1  # For quantization if needed

# Audio processing
pyaudio==0.2.11
numpy==1.24.3
scipy==1.10.1

# Image processing
pillow==10.0.0
opencv-python==4.8.0.74

# Data handling
requests==2.31.0
python-dotenv==1.0.0

# Development and monitoring
psutil==5.9.5  # For system monitoring
nvidia-ml-py3==7.352.0  # For GPU monitoring

# Optional: NVIDIA optimizations
# nvidia-cublas-cu12
# nvidia-cuda-runtime-cu12
# nvidia-cudnn-cu12
